[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ap_assembler"
version = "0.1.0"
description = "Python implementation of the Xerox/Telefile Assembly Program (AP)"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
